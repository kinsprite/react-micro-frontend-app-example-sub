import React from 'react';

import styles from './LazyInfo.module.pcss';

function LazyInfo() : JSX.Element {
  return <div className={styles.container}>This is lazy content in App Example Sub.</div>;
}

export default LazyInfo;
